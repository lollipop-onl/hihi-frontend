import React, { Component } from 'react';

import Router from '../components/Router';

class Root extends Component {
    render() {
      return (
        <Router />
      );
    }
}

Root.propTypes = {
};

export default Root;
